# =============================================================================
# @author dalmoorii (dalmoori@gmail.com) : 2026-01-13, initial version
# 
#   .env를 사용하면 민감 정보를 직접 코드에 하드코딩하지 않아도 되므로, 
#   다른 팀원이 사용할 때 각자의 .env만 수정하면 된다.
#   보안 측면에서 .env 파일을 .gitignore에 추가하도록 한다. 
#   민감한 비밀번호가 공용 저장소에 올라가는 것을 방지. (필수!)
# -----------------------------------------------------------------------------
#   팁: .env 값을 Jenkins에 넣는 법
#      - .env 파일의 값을 Jenkins가 기동될 때 주입하고 싶다면, 
#         setup-all.sh에서 Jenkins 컨테이너를 실행할 때 
#         --env GITHUB_REPO_URL=${GITHUB_REPO_URL} 
#         옵션을  추가하면 Jenkins 시스템 전체에서 바로 사용할 수 있다.
# =============================================================================

# ---------------------------------------------------------
# 공통 인프라 설정 (Shared Library용)
# ---------------------------------------------------------

# Shared Library가 위치한 인프라 저장소 주소 (setup-jenkins-lib.sh에서 사용)
GITHUB_PLATFORM_REPO_URL="https://github.com/kyungseo/platform-infra.git"

# ---------------------------------------------------------
# 멀티 앱별 GitHub 저장소 주소
# ---------------------------------------------------------

# setup-all.sh의 APPS=("sample-backend-api" "auth-service") 리스트와 매칭
# 규칙: {APP_NAME}_URL (대문자 변환, 하이픈은 언더바로 변경)

SAMPLE_BACKEND_API_URL="https://github.com/kyungseo/sample-backend-api.git"
AUTH_SERVICE_URL="https://github.com/kyungseo/auth-service.git"

# ---------------------------------------------------------
# 단일 앱일 경우 설졍
# ---------------------------------------------------------

# Demo App
APP_NAME=demo-app
APP_PORT=8089

# ---------------------------------------------------------
# 데이터베이스 및 미들웨어 설정
# ---------------------------------------------------------
POSTGRES_DB="demodb"
POSTGRES_USER="admin"
POSTGRES_PASS="admin123"

# ---------------------------------------------------------
# Jenkins 설정
# ---------------------------------------------------------
JENKINS_URL="http://localhost:8080"
JENKINS_USER="admin"
JENKINS_PASS="admin123"

# ---------------------------------------------------------
# Nexus 설정
# ---------------------------------------------------------
NEXUS_URL="http://localhost:8081"
NEXUS_USER="admin"
NEXUS_PASS="admin123"
NEXUS_DOCKER_PORT=5001

# ---------------------------------------------------------
# GitHub 공통 자격 증명 (Credentials 생성용)
# ---------------------------------------------------------
GITHUB_USER="kyungseo"
GITHUB_TOKEN="TOKEN" # 실제 TOKEN 값을 넣을 것